// User.jsx
import React from 'react';
import AccountSection from '../../components/AccountSection';

const User = () => {
    return (
            <AccountSection />
    );
};

export default User;

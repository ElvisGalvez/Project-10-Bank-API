import React from 'react';
import AccountSection from '../../components/AccountSection';

const Profile = () => {
    return (
            <AccountSection />
    );
};

export default Profile;
